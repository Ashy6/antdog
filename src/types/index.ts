export enum Resource {
  article = "article",
  banner = "banner",
  card = "card",
  contact = "contact",
  transaction = "transaction",
  version = "version",
  withdraw = "withdraw",
}

export enum Nav {
  home = "home",
  trade = "trade",
  us = "about-us",
}
